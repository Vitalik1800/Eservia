<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/windowBackgroundDefault"
    app:layout_scrollFlags="scroll">

    <RelativeLayout
        android:id="@+id/rlPaymentBar"
        style="@style/CommonCardHolderStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.cardview.widget.CardView
            android:id="@+id/cvPaymentBar"
            style="@style/CommonCardStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardCornerRadius="0dp">

            <include
                android:id="@+id/layout_payment_bar"
                layout="@layout/layout_payment_bar" />

        </androidx.cardview.widget.CardView>

    </RelativeLayout>

    <TextView
        android:id="@+id/tvSelectCategory"
        style="@style/CommonTextTitle"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="2dp"
        android:text="@string/choose_category"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/rlPaymentBar" />

</androidx.constraintlayout.widget.ConstraintLayout>
